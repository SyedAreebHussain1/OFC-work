import { connect } from "react-redux";
import { PlanBuilder } from "./plan-builder"


const mapStateToProps = (state) => ({
 
  });
  const mapDispatchToPrpos = (dispatch) => {
    return {
     
    };
  };
export default connect(mapStateToProps, mapDispatchToPrpos)(PlanBuilder);
