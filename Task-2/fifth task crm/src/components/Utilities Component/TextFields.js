import React from "react";
import { Label, Input, FormText } from "reactstrap";

export const TextFields = ({type , name , id , placeholder}) => {
  <>
    <Input
      type="email"
      name="email"
      id="exampleEmail"
      placeholder="with a placeholder"
    />
  </>;
};
